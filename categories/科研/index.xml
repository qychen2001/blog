<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>科研 on 光头的自留地</title><link>https://qychen2001.github.io/blog/categories/%E7%A7%91%E7%A0%94/</link><description>Recent content in 科研 on 光头的自留地</description><generator>Hugo -- gohugo.io</generator><language>zh-cn</language><lastBuildDate>Wed, 18 Jun 2025 00:00:00 +0000</lastBuildDate><atom:link href="https://qychen2001.github.io/blog/categories/%E7%A7%91%E7%A0%94/index.xml" rel="self" type="application/rss+xml"/><item><title>DPO 的闭式最优解推导</title><link>https://qychen2001.github.io/blog/p/dpo-%E7%9A%84%E9%97%AD%E5%BC%8F%E6%9C%80%E4%BC%98%E8%A7%A3%E6%8E%A8%E5%AF%BC/</link><pubDate>Wed, 18 Jun 2025 00:00:00 +0000</pubDate><guid>https://qychen2001.github.io/blog/p/dpo-%E7%9A%84%E9%97%AD%E5%BC%8F%E6%9C%80%E4%BC%98%E8%A7%A3%E6%8E%A8%E5%AF%BC/</guid><description>&lt;h2 id="先回顾传统-rlhf-在优化什么"&gt;先回顾：传统 RLHF 在优化什么？
&lt;/h2&gt;&lt;p&gt;RLHF（Reinforcement Learning from Human Feedback）的核心目标是让大模型生成的回答 &lt;strong&gt;更符合人类偏好&lt;/strong&gt;，同时 &lt;strong&gt;不要偏离原始 SFT 模型太远&lt;/strong&gt;（避免幻觉、胡说八道）。&lt;/p&gt;
&lt;p&gt;数学上，这个目标通常写成：&lt;/p&gt;
$$
J(\pi) = \mathbb{E}_{x \sim \mathcal{D},\, y \sim \pi(\cdot|x)} \Big[ r(x,y) \Big] - \beta \, \mathrm{KL}\big(\pi(\cdot|x) \parallel \pi_{\text{ref}}(\cdot|x)\big)
$$&lt;ul&gt;
&lt;li&gt;第一项：期望奖励最大化（$r(x,y)$ 是 Reward Model 给出的分数）。&lt;/li&gt;
&lt;li&gt;第二项：KL 散度惩罚，强迫新策略 $\pi$ 不能离参考模型（通常是 SFT 后的模型）$\pi_{\text{ref}}$ 太远。&lt;/li&gt;
&lt;li&gt;$\beta$ 是超参数，控制惩罚强度（$\beta$ 越大，模型越“保守”）。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;传统做法是用 PPO（或其它 RL 算法）去最大化这个 $J(\pi)$。但 PPO 训练不稳定、采样开销大，还需要单独训练一个 Reward Model。&lt;strong&gt;DPO 的天才之处在于：它发现这个目标其实有解析解！&lt;/strong&gt;&lt;/p&gt;
&lt;h2 id="直观理解最优策略长什么样"&gt;直观理解：最优策略长什么样？
&lt;/h2&gt;&lt;p&gt;想象一下：对于一个固定的问题 $x$，我们希望生成的回答 $y$ 既得分高（$r(x,y)$ 大），又不能离 $\pi_{\text{ref}}$ 太远。&lt;/p&gt;
&lt;p&gt;最优策略应该怎么做？&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;对奖励高的回答，&lt;strong&gt;指数级地提高&lt;/strong&gt;它的概率；&lt;/li&gt;
&lt;li&gt;对奖励低的回答，&lt;strong&gt;指数级地压低&lt;/strong&gt;它的概率；&lt;/li&gt;
&lt;li&gt;同时用 $\pi_{\text{ref}}$ 做“锚点”保持稳定性。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这听起来像 softmax，但奖励被指数放大了！&lt;br&gt;
数学上，我们猜测最优策略的形式是：&lt;/p&gt;
$$
\pi^*(y|x) \propto \pi_{\text{ref}}(y|x) \cdot \exp\left( \frac{r(x,y)}{\beta} \right)
$$&lt;p&gt;归一化之后就得到&lt;strong&gt;闭式关系&lt;/strong&gt;（closed-form relation）：&lt;/p&gt;
$$
\pi^*(y|x) = \frac{1}{Z(x)} \, \pi_{\text{ref}}(y|x) \, \exp\left( \frac{r(x,y)}{\beta} \right)
$$&lt;p&gt;其中 $Z(x)$ 是归一化常数（partition function）：&lt;/p&gt;
$$
Z(x) = \sum_{y} \pi_{\text{ref}}(y|x) \exp\left( \frac{r(x,y)}{\beta} \right)
$$&lt;p&gt;（如果是连续 token 分布，就换成积分。）&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;一句话直觉&lt;/strong&gt;：最优策略 = 参考模型 × “奖励指数放大器”。$\beta$ 控制放大器的强度。&lt;/p&gt;
&lt;h2 id="严谨推导为什么这个形式是最优的"&gt;严谨推导：为什么这个形式是最优的？
&lt;/h2&gt;&lt;p&gt;现在我们来证明上面的猜想是&lt;strong&gt;唯一&lt;/strong&gt;的最优解。固定一个 $x$，目标简化为最大化内层期望：&lt;/p&gt;
$$
\max_{\pi(\cdot|x)} \ \mathbb{E}_{y \sim \pi} \left[ r(x,y) - \beta \log \frac{\pi(y|x)}{\pi_{\text{ref}}(y|x)} \right]
$$&lt;p&gt;这是一个带约束的优化问题（$\sum_y \pi(y|x) = 1$）。我们构造 Lagrangian：&lt;/p&gt;
$$
L(\pi, \lambda) = \mathbb{E}_{y\sim\pi} \Big[ r(x,y) - \beta \log \frac{\pi(y|x)}{\pi_{\text{ref}}(y|x)} \Big] + \lambda \left(1 - \sum_y \pi(y|x)\right)
$$&lt;p&gt;对 $\pi(y|x)$ 求变分导数（functional derivative）并令其为零：&lt;/p&gt;
$$
\frac{\partial L}{\partial \pi(y|x)} = r(x,y) - \beta \log \frac{\pi(y|x)}{\pi_{\text{ref}}(y|x)} - \beta - \lambda = 0
$$&lt;p&gt;解得：&lt;/p&gt;
$$
\log \pi(y|x) = \log \pi_{\text{ref}}(y|x) + \frac{r(x,y)}{\beta} - 1 - \frac{\lambda}{\beta}
$$&lt;p&gt;两边取指数并归一化（$\lambda$ 由归一化条件决定），正好得到我们之前的闭式关系：&lt;/p&gt;
$$
\boxed{\pi^*(y|x) = \frac{1}{Z(x)} \, \pi_{\text{ref}}(y|x) \, \exp\left( \frac{r(x,y)}{\beta} \right)}
$$&lt;p&gt;这个推导其实是指数族分布的经典结果：在 KL 正则化下，&lt;strong&gt;最优分布一定是参考分布乘以指数加权的似然&lt;/strong&gt;。DPO 只是把它应用到了语言模型上。&lt;/p&gt;
&lt;h2 id="从最优策略反推奖励函数"&gt;从最优策略反推奖励函数
&lt;/h2&gt;&lt;p&gt;现在我们把闭式关系“反过来用”。对两边取对数：&lt;/p&gt;
$$
\log \frac{\pi^*(y|x)}{\pi_{\text{ref}}(y|x)} = \frac{r(x,y)}{\beta} - \log Z(x)
$$&lt;p&gt;两边乘 $\beta$ 得到：&lt;/p&gt;
$$
r(x,y) = \beta \log \frac{\pi^*(y|x)}{\pi_{\text{ref}}(y|x)} + \beta \log Z(x)
$$&lt;p&gt;关键观察来了：&lt;strong&gt;$\beta \log Z(x)$ 只依赖于输入 $x$，与具体回答 $y$ 无关&lt;/strong&gt;。&lt;/p&gt;
&lt;p&gt;在人类偏好数据里，我们总是成对比较两个回答（chosen $y_w$ 和 rejected $y_l$）。此时 $Z(x)$ 在两个回答上&lt;strong&gt;完全抵消&lt;/strong&gt;：&lt;/p&gt;
$$
r(x,y_w) - r(x,y_l) = \beta \log \frac{\pi^*(y_w|x)}{\pi_{\text{ref}}(y_w|x)} - \beta \log \frac{\pi^*(y_l|x)}{\pi_{\text{ref}}(y_l|x)}
$$&lt;p&gt;这意味着：&lt;strong&gt;我们可以直接把策略模型 $\pi_\theta$ 本身当成一个隐式的 Reward Model&lt;/strong&gt;！&lt;/p&gt;
&lt;p&gt;定义隐式奖励：&lt;/p&gt;
$$
r_\theta(x,y) \triangleq \beta \log \frac{\pi_\theta(y|x)}{\pi_{\text{ref}}(y|x)}
$$&lt;h2 id="自然导出-dpo-损失函数"&gt;自然导出 DPO 损失函数
&lt;/h2&gt;&lt;p&gt;有了这个等价关系，DPO 就水到渠成了。我们把隐式奖励代入 Bradley-Terry 偏好模型（人类更喜欢 $y_w$ 胜过 $y_l$ 的概率）：&lt;/p&gt;
$$
P(y_w \succ y_l | x) = \sigma \big( r(x,y_w) - r(x,y_l) \big)
$$&lt;p&gt;代入隐式奖励，得到 DPO 的最终损失（负对数似然）：&lt;/p&gt;
$$
\mathcal{L}_{\text{DPO}}(\pi_\theta) = -\mathbb{E}_{(x,y_w,y_l) \sim \mathcal{D}} \left[ \log \sigma \left( \beta \log \frac{\pi_\theta(y_w|x)}{\pi_{\text{ref}}(y_w|x)} - \beta \log \frac{\pi_\theta(y_l|x)}{\pi_{\text{ref}}(y_l|x)} \right) \right]
$$&lt;p&gt;一句话总结：&lt;strong&gt;DPO 直接让策略提高 chosen 相对 reference 的概率，同时压低 rejected 相对 reference 的概率&lt;/strong&gt;，整个过程只用一个模型、一次 offline 训练，干净、稳定、高效。&lt;/p&gt;
&lt;h2 id="附录变分导数这一步的详细推导"&gt;附录：变分导数这一步的详细推导
&lt;/h2&gt;&lt;h3 id="背景知识"&gt;背景知识
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;我们优化的对象不是模型参数 $\theta$，而是&lt;strong&gt;整个概率分布函数&lt;/strong&gt; $\pi(\cdot|x)$，这属于&lt;strong&gt;变分学&lt;/strong&gt;（calculus of variations）。&lt;/li&gt;
&lt;li&gt;语言模型是离散的，但 RL 文献常用泛函导数记号，因为它对连续动作空间也成立，且更简洁。&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="离散情况最推荐的直观方式"&gt;离散情况（最推荐的直观方式）
&lt;/h3&gt;&lt;p&gt;固定 $x$，令 $p_y = \pi(y|x)$，$q_y = \pi_{\rm ref}(y|x)$，$r_y = r(x,y)$。目标：&lt;/p&gt;
$$
J(p) = \sum_y p_y r_y - \beta \sum_y p_y \log \frac{p_y}{q_y}, \quad \sum_y p_y = 1
$$&lt;p&gt;Lagrangian：&lt;/p&gt;
$$
L = \sum_y p_y r_y - \beta \sum_y p_y (\log p_y - \log q_y) + \lambda (1 - \sum_y p_y)
$$&lt;p&gt;对&lt;strong&gt;某个具体&lt;/strong&gt; $p_k$ 求偏导 $\frac{\partial L}{\partial p_k}$：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;$p_k r_k$ → 贡献 $r_k$&lt;/li&gt;
&lt;li&gt;$-\beta p_k \log p_k$ → 贡献 $-\beta (\log p_k + 1)$ （关键！因为 $\frac{d}{dp}(p \log p) = \log p + 1$）&lt;/li&gt;
&lt;li&gt;$+\beta p_k \log q_k$ → 贡献 $+\beta \log q_k$&lt;/li&gt;
&lt;li&gt;约束 → 贡献 $-\lambda$&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;合起来：&lt;/p&gt;
$$
r_k - \beta \log \frac{p_k}{q_k} - \beta - \lambda = 0
$$&lt;p&gt;这个“-$\beta$”正是来自负熵项 $p \log p$ 的导数特性。解出 $p_k \propto q_k \exp(r_k / \beta)$，归一化即得闭式解。&lt;/p&gt;
&lt;h3 id="连续情况泛函导数"&gt;连续情况（泛函导数）
&lt;/h3&gt;&lt;p&gt;目标写成积分形式。给 $\pi$ 一个微小扰动 $\pi_\epsilon = \pi + \epsilon \eta$（$\int \eta = 0$），要求变分为零：&lt;/p&gt;
&lt;p&gt;计算后同样得到：&lt;/p&gt;
$$
r(y) - \beta \log \frac{\pi(y)}{q(y)} - \beta - \lambda = 0
$$&lt;p&gt;&lt;strong&gt;离散和连续结果完全一致&lt;/strong&gt;。那个“-$\beta$”项在两种情况下都必然出现。&lt;/p&gt;
&lt;h3 id="小扰动直观法无需记住公式"&gt;小扰动直观法（无需记住公式）
&lt;/h3&gt;&lt;p&gt;在某个 $y$ 上多给一点概率 $\delta p$（同时在别处减掉保持归一化），目标变化必须为零：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;奖励：$+\delta p \cdot r(y)$&lt;/li&gt;
&lt;li&gt;KL 变化：$\delta p \cdot (-\beta \log \frac{\pi}{q}) - \beta \delta p$&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;平衡后得到完全相同的条件。&lt;/p&gt;
&lt;p&gt;这个技巧来自&lt;strong&gt;最大熵强化学习&lt;/strong&gt;（MaxEnt RL）和 TRPO/PPO 的理论基础，是现代对齐方法的共同基石。&lt;/p&gt;</description></item><item><title>详细梳理 PPO/DPO 到 GRPO 及其变体</title><link>https://qychen2001.github.io/blog/p/%E8%AF%A6%E7%BB%86%E6%A2%B3%E7%90%86-ppo/dpo-%E5%88%B0-grpo-%E5%8F%8A%E5%85%B6%E5%8F%98%E4%BD%93/</link><pubDate>Sun, 15 Jun 2025 00:00:00 +0000</pubDate><guid>https://qychen2001.github.io/blog/p/%E8%AF%A6%E7%BB%86%E6%A2%B3%E7%90%86-ppo/dpo-%E5%88%B0-grpo-%E5%8F%8A%E5%85%B6%E5%8F%98%E4%BD%93/</guid><description>&lt;h2 id="引言后训练真正优化的是什么"&gt;引言：后训练真正优化的是什么？
&lt;/h2&gt;&lt;p&gt;大语言模型的训练通常可以粗略分成两个阶段：预训练和后训练。预训练让模型从大规模语料中学习语言、知识与基本推理模式；后训练则试图回答另一个问题：&lt;strong&gt;在已经会生成文本的基础上，如何让模型更符合人类偏好，或者更擅长完成某类任务？&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;在今天的 LLM 训练语境里，后训练不只是简单的监督微调。SFT 可以让模型模仿高质量答案，但它本质上仍然是在做行为克隆：给定输入，学习参考答案的分布。如果我们关心的是“哪个回答更好”“哪个推理更可靠”“哪个答案更符合人类偏好”，那么仅仅模仿一个固定答案就不够了。这也是 RLHF、DPO、GRPO 等方法出现的背景。&lt;/p&gt;
&lt;p&gt;这篇文章想梳理的不是“强化学习是什么”，而是下面这条技术主线：&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;PPO 是如何把偏好信号转化成在线策略优化的；DPO 为什么可以绕过显式奖励模型和 RL 过程；GRPO 又为什么在推理模型训练中重新回到在线采样，但去掉了 PPO 中昂贵的价值模型。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;因此，本文会尽量减少类比和科普式解释，直接从目标函数、训练数据和工程取舍出发，讨论 PPO、DPO 和 GRPO 三者之间的关系。&lt;/p&gt;
&lt;h2 id="从-rlhf-的统一目标开始"&gt;从 RLHF 的统一目标开始
&lt;/h2&gt;&lt;p&gt;无论是 PPO、DPO 还是 GRPO，它们都可以看作是在回答同一个问题：给定一个输入 $x$，我们希望训练后的策略模型 $\pi_\theta(y|x)$ 生成更高质量的输出 $y$，同时不要偏离原始参考模型 $\pi_{\text{ref}}(y|x)$ 太远。&lt;/p&gt;
&lt;p&gt;一个常见的 KL 正则化 RLHF 目标可以写成：&lt;/p&gt;
$$
\max_\pi \; \mathbb{E}_{x \sim \mathcal{D},\, y \sim \pi(\cdot|x)} \left[ r(x,y) \right] - \beta \, \mathbb{D}_{\mathrm{KL}}\left( \pi(y|x) \, \|\, \pi_{\text{ref}}(y|x) \right)
$$&lt;p&gt;其中：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;$\pi_\theta$ 是当前要训练的策略模型；&lt;/li&gt;
&lt;li&gt;$\pi_{\text{ref}}$ 通常是 SFT 模型或训练前的旧策略，用来约束模型不要偏离太远；&lt;/li&gt;
&lt;li&gt;$r(x,y)$ 是奖励函数，可以来自奖励模型，也可以来自规则打分；&lt;/li&gt;
&lt;li&gt;$\beta$ 控制奖励最大化和 KL 约束之间的权衡。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这个目标函数非常关键。PPO、DPO 和 GRPO 的差异，不在于它们想优化的方向完全不同，而在于它们对这个目标采取了不同的实现路径：&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;方法&lt;/th&gt;
&lt;th style="text-align: right"&gt;是否需要奖励模型&lt;/th&gt;
&lt;th style="text-align: right"&gt;是否需要在线采样&lt;/th&gt;
&lt;th style="text-align: right"&gt;是否需要价值模型 / critic&lt;/th&gt;
&lt;th&gt;核心思想&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;PPO&lt;/td&gt;
&lt;td style="text-align: right"&gt;通常需要&lt;/td&gt;
&lt;td style="text-align: right"&gt;需要&lt;/td&gt;
&lt;td style="text-align: right"&gt;需要&lt;/td&gt;
&lt;td&gt;用 actor-critic 估计优势并进行稳定的策略更新&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;DPO&lt;/td&gt;
&lt;td style="text-align: right"&gt;不需要显式奖励模型&lt;/td&gt;
&lt;td style="text-align: right"&gt;不需要&lt;/td&gt;
&lt;td style="text-align: right"&gt;不需要&lt;/td&gt;
&lt;td&gt;把偏好优化改写成二分类形式，直接优化 chosen/rejected 对&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;GRPO&lt;/td&gt;
&lt;td style="text-align: right"&gt;可用奖励模型或规则奖励&lt;/td&gt;
&lt;td style="text-align: right"&gt;需要&lt;/td&gt;
&lt;td style="text-align: right"&gt;不需要&lt;/td&gt;
&lt;td&gt;用同一 prompt 下的一组回答估计相对优势，替代 critic&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;下面依次展开。&lt;/p&gt;
&lt;h2 id="奖励模型把偏好数据变成标量奖励"&gt;奖励模型：把偏好数据变成标量奖励
&lt;/h2&gt;&lt;p&gt;经典 RLHF 的第一步通常是训练奖励模型。训练数据不是单个标准答案，而是偏好对：&lt;/p&gt;
$$
(x, y_w, y_l),
$$&lt;p&gt;其中 $y_w$ 表示被人类或某种标注机制认为更好的回答，$y_l$ 表示较差的回答。奖励模型 $r_\phi(x,y)$ 的目标是给更好的回答更高分。&lt;/p&gt;
&lt;p&gt;常用的 Bradley-Terry 形式可以写成：&lt;/p&gt;
$$
P(y_w \succ y_l | x) = \sigma\left(r_\phi(x,y_w) - r_\phi(x,y_l)\right),
$$&lt;p&gt;对应的损失函数为：&lt;/p&gt;
$$
\mathcal{L}_{\text{RM}}(\phi)
= - \mathbb{E}_{(x,y_w,y_l) \sim \mathcal{D}}
\left[\log \sigma\left(r_\phi(x,y_w) - r_\phi(x,y_l)\right)\right].
$$&lt;p&gt;训练完成后，奖励模型会被冻结，作为后续策略优化的打分器。PPO 和 GRPO 都可以使用这个奖励模型；而 DPO 的一个关键变化是：它不再显式训练这个奖励模型，而是把奖励模型隐含进策略模型本身。&lt;/p&gt;
&lt;h2 id="ppo显式奖励模型--价值模型--在线策略优化"&gt;PPO：显式奖励模型 + 价值模型 + 在线策略优化
&lt;/h2&gt;&lt;p&gt;PPO 是早期 LLM RLHF 中非常重要的一条路线。它的基本结构是 actor-critic：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;actor / policy model&lt;/strong&gt;：当前训练的语言模型 $\pi_\theta$；&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;reference model&lt;/strong&gt;：参考模型 $\pi_{\text{ref}}$，用于 KL 约束；&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;reward model&lt;/strong&gt;：冻结的奖励模型 $r_\phi$，用于给完整回答打分；&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;value model / critic&lt;/strong&gt;：价值模型 $V_\psi$，用于估计当前状态的期望回报，从而计算 advantage。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;在 LLM 场景中，一个 prompt $x$ 对应一条生成轨迹：&lt;/p&gt;
$$
y = (a_1, a_2, \dots, a_T),
$$&lt;p&gt;其中每个 token $a_t$ 都可以看成一步 action，状态 $s_t$ 则是 prompt 加上此前已经生成的 token。&lt;/p&gt;
&lt;h3 id="ppo-的训练流程"&gt;PPO 的训练流程
&lt;/h3&gt;&lt;p&gt;PPO 在 LLM 后训练中的流程大致如下：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;从 prompt 数据集中采样一批输入 $x$；&lt;/li&gt;
&lt;li&gt;用当前策略 $\pi_\theta$ 生成回答 $y$；&lt;/li&gt;
&lt;li&gt;用奖励模型 $r_\phi(x,y)$ 给完整回答打分；&lt;/li&gt;
&lt;li&gt;额外计算当前策略相对参考策略的 KL 惩罚；&lt;/li&gt;
&lt;li&gt;用价值模型 $V_\psi(s_t)$ 估计每个 token 位置的 advantage；&lt;/li&gt;
&lt;li&gt;用 PPO clipped objective 更新策略模型；&lt;/li&gt;
&lt;li&gt;同时更新价值模型，使它更好地预测未来回报。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;这里最重要的量是 advantage。PPO 并不是简单地说“这条回答 reward 高，所以整条回答都应该被强化”，而是希望知道：在生成过程的每一个 token 位置，当前模型选择的这个 token 相对于它在该状态下的平均表现，到底是更好还是更差。&lt;/p&gt;
&lt;p&gt;形式上，advantage 定义为：&lt;/p&gt;
$$
A_t = Q(s_t,a_t) - V(s_t).
$$&lt;p&gt;这里的 $s_t$ 表示当前生成状态，也就是 prompt 加上已经生成出来的前缀；$a_t$ 表示当前位置生成的 token。$Q(s_t,a_t)$ 表示在状态 $s_t$ 下选择 token $a_t$ 之后，未来整条生成所能得到的期望回报；$V(s_t)$ 表示在状态 $s_t$ 下，按照当前策略继续生成所能得到的平均期望回报。&lt;/p&gt;
&lt;p&gt;因此，$A_t$ 衡量的不是“这个 token 的绝对好坏”，而是：&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;在同一个上下文状态下，选择这个 token 是否比当前策略的平均选择更好。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;如果 $A_t &amp;gt; 0$，说明这个 token 选择比当前策略的平均水平更好，训练时应该提高它的概率；如果 $A_t &amp;lt; 0$，说明这个 token 选择低于平均水平，训练时应该降低它的概率。&lt;/p&gt;
&lt;p&gt;问题在于，真实的 $Q(s_t,a_t)$ 很难直接获得。对于 LLM 来说，奖励模型通常只会在完整回答生成结束后给一个 sequence-level reward，例如“这整段回答得分为 0.8”。但 PPO 的策略更新发生在 token 级别：模型需要知道第 1 个 token、第 2 个 token、一直到第 $T$ 个 token 分别应该被增强还是削弱。于是就需要一个方法，把最终的 sequence-level reward 转化为每个 token 位置上的训练信号。&lt;/p&gt;
&lt;p&gt;这就是价值模型 $V_\psi(s_t)$ 的作用。它负责预测：给定当前已经生成到一半的状态 $s_t$，如果模型继续生成下去，最终大概能得到多少回报。换句话说，value model 是一个对“当前前缀未来质量”的估计器。&lt;/p&gt;
&lt;p&gt;有了价值模型之后，就可以定义 temporal-difference error，也就是 TD error：&lt;/p&gt;
$$
\delta_t = r_t + \gamma V_\psi(s_{t+1}) - V_\psi(s_t).
$$&lt;p&gt;这个式子的含义是：&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;生成 token $a_t$ 之后，模型对未来回报的估计有没有变好。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;其中，$V_\psi(s_t)$ 是生成当前 token 之前，对最终回报的预测；$V_\psi(s_{t+1})$ 是生成当前 token 之后，对最终回报的预测；$r_t$ 是当前位置获得的即时奖励；$\gamma$ 是折扣因子。&lt;/p&gt;
&lt;p&gt;在普通强化学习中，每一步 action 都可能获得即时奖励；但在 LLM 的 RLHF 里，主要奖励通常来自完整回答结束之后的奖励模型打分。因此很多中间 token 的 $r_t$ 可能接近 0，而最后一个位置才拿到主要 reward。当然，实际实现中还常常会把 KL penalty 作为 token-level reward 加进去，用来约束当前模型不要偏离 reference model 太远。&lt;/p&gt;
&lt;p&gt;如果某个 token 生成之后，价值模型对未来最终回报的预测上升了，也就是 $V_\psi(s_{t+1}) &amp;gt; V_\psi(s_t)$，那么 $\delta_t$ 会偏正，说明这个 token 可能把生成带向了更好的方向；反之，如果生成这个 token 后，未来回报预测下降，$\delta_t$ 就会偏负。&lt;/p&gt;
&lt;p&gt;不过，只看一步 TD error 仍然不够稳定。一个 token 的好坏不一定会在下一步立刻体现出来，它可能要经过后面很多 token 才能反映到最终答案质量上。例如在数学推理中，前面某一步推理是否正确，可能要到最终答案才看得出来。&lt;/p&gt;
&lt;p&gt;因此，PPO 通常使用 GAE（Generalized Advantage Estimation）来把多步 TD error 累积起来：&lt;/p&gt;
$$
A_t^{\text{GAE}}
= \sum_{k=0}^{T-t-1} (\gamma \lambda)^k \delta_{t+k}.
$$&lt;p&gt;这个式子可以理解成：当前位置 $t$ 的 advantage，不只看当前一步的 TD error $\delta_t$，还会继续看后续若干步的 TD error：&lt;/p&gt;
$$
\delta_t,\quad \delta_{t+1},\quad \delta_{t+2},\quad \dots
$$&lt;p&gt;越靠后的 TD error，对当前位置的影响越小，由 $(\gamma\lambda)^k$ 控制衰减。这里的 $\lambda$ 是一个偏差和方差之间的折中参数：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;当 $\lambda = 0$ 时，GAE 只看一步 TD error，方差较低，但偏差较大；&lt;/li&gt;
&lt;li&gt;当 $\lambda = 1$ 时，GAE 更接近使用完整轨迹回报，偏差较低，但方差较大；&lt;/li&gt;
&lt;li&gt;实际训练中通常取二者之间的值，让 advantage 估计既不过于短视，也不过于依赖高方差的完整回报。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;所以，GAE 的作用可以概括为：&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;用价值模型提供 baseline，用多步 TD error 把完整回答的奖励信号向前传播，最终为每个 token 得到一个相对稳定的 advantage 估计。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;这也是 PPO 在 LLM 后训练中需要 critic / value model 的核心原因。奖励模型只告诉我们“整条回答好不好”，而 PPO 需要知道“生成这条回答时，每个 token 的选择应该被强化还是削弱”。价值模型和 GAE 正是用来完成这个 credit assignment 的。&lt;/p&gt;
&lt;h3 id="ppo-的-clipped-objective"&gt;PPO 的 clipped objective
&lt;/h3&gt;&lt;p&gt;PPO 的策略更新不是直接最大化 reward，而是使用一个被裁剪的概率比率来限制更新幅度。定义：&lt;/p&gt;
$$
\rho_t(\theta) =
\frac{\pi_\theta(a_t|s_t)}{\pi_{\theta_{\text{old}}}(a_t|s_t)}.
$$&lt;p&gt;PPO 的 clipped surrogate objective 为：&lt;/p&gt;
$$
\mathcal{L}_{\text{clip}}(\theta) = \mathbb{E}_t \left[\min \left(\rho_t(\theta) A_t,\operatorname{clip}(\rho_t(\theta), 1-\epsilon, 1+\epsilon) A_t\right)\right].
$$&lt;p&gt;这个裁剪项的作用是限制策略更新幅度，避免模型因为某一批 reward 信号过强而发生过大的分布漂移。&lt;/p&gt;
&lt;p&gt;在 RLHF 中，最终的 PPO 训练目标通常还会加入 KL 惩罚和 value loss：&lt;/p&gt;
$$
\mathcal{L}_{\text{PPO}} = \mathcal{L}_{\text{clip}} - \beta \, \mathbb{D}_{\mathrm{KL}}(\pi_\theta \, \| \, \pi_{\text{ref}}) - c_v \mathcal{L}_{\text{value}} + c_e \, \mathcal{H}(\pi_\theta)
$$&lt;p&gt;
其中 value loss 通常是：&lt;/p&gt;
$$
\mathcal{L}_{\text{value}}
= \mathbb{E}_t \left[\left(V_\psi(s_t) - R_t\right)^2\right].
$$&lt;h3 id="ppo-的问题稳定但昂贵"&gt;PPO 的问题：稳定但昂贵
&lt;/h3&gt;&lt;p&gt;PPO 的优点是通用、稳定，并且可以直接优化在线采样得到的 reward。只要我们能定义奖励函数，PPO 就可以尝试把策略往高 reward 区域推。&lt;/p&gt;
&lt;p&gt;但它的工程成本也很明显：&lt;/p&gt;
&lt;p&gt;第一，PPO 需要维护多个模型。除了当前策略模型，还需要 reference model、reward model 和 value model。对于大语言模型来说，value model 往往也是一个体量接近策略模型的网络，这会显著增加显存和训练复杂度。&lt;/p&gt;
&lt;p&gt;第二，PPO 是在线 RL。训练时要不断采样、打分、估计 advantage、更新策略，再继续采样。这比普通监督微调更复杂，也更容易受到 reward hacking、KL 控制、采样温度和 advantage 估计质量的影响。&lt;/p&gt;
&lt;p&gt;第三，PPO 的 token-level credit assignment 并不自然。奖励模型通常只对完整回答给一个标量分数，但策略更新发生在 token 级别，因此需要 critic 把最终奖励转化成每个 token 的优势估计。&lt;/p&gt;
&lt;p&gt;这些问题直接引出了两条不同方向的简化路线：DPO 和 GRPO。&lt;/p&gt;
&lt;h2 id="dpo把-rlhf-改写成直接偏好分类"&gt;DPO：把 RLHF 改写成直接偏好分类
&lt;/h2&gt;&lt;p&gt;DPO（Direct Preference Optimization）的出发点非常不同。它并不是在 PPO 的基础上继续改进 advantage 估计，而是重新审视 KL 正则化 RLHF 目标本身。&lt;/p&gt;
&lt;p&gt;回到前面的目标：&lt;/p&gt;
$$
\max_\pi \; \mathbb{E}_{x \sim \mathcal{D},\, y \sim \pi(\cdot|x)} \left[ r(x,y) \right] - \beta \, \mathbb{D}_{\mathrm{KL}}\left( \pi(y|x) \, \|\, \pi_{\text{ref}}(y|x) \right)
$$&lt;p&gt;DPO 的关键观察是：在这个目标下，最优策略和奖励函数之间存在一个 closed-form 关系。忽略只与 $x$ 有关的归一化项，可以得到：&lt;/p&gt;
$$
r(x,y) = \beta \log \frac{\pi^*(y|x)}{\pi_{\text{ref}}(y|x)} + C(x).
$$&lt;p&gt;这意味着：如果我们把策略模型本身看成隐式奖励模型，那么就不一定需要先训练一个显式的 reward model，再用 PPO 去优化它。我们可以直接用偏好数据训练 $\pi_\theta$，让它提高 chosen response 相对 reference model 的概率，同时降低 rejected response 的相对概率。&lt;/p&gt;
&lt;h3 id="dpo-的目标函数"&gt;DPO 的目标函数
&lt;/h3&gt;&lt;p&gt;给定偏好数据 $(x, y_w, y_l)$，DPO 的损失函数为：&lt;/p&gt;
$$
\mathcal{L}_{\text{DPO}}(\theta)
= -\mathbb{E}_{(x,y_w,y_l)\sim \mathcal{D}}
\left[
\log \sigma \Bigl(
\beta \Bigl(
\log \frac{\pi_\theta(y_w|x)}{\pi_{\text{ref}}(y_w|x)}
-
\log \frac{\pi_\theta(y_l|x)}{\pi_{\text{ref}}(y_l|x)}
\Bigr)
\Bigr)
\right].
$$&lt;p&gt;这个式子可以拆开理解。DPO 比较的不是 $\pi_\theta(y_w|x)$ 和 $\pi_\theta(y_l|x)$ 的绝对概率，而是它们相对于参考模型 $\pi_{\text{ref}}$ 的概率提升：&lt;/p&gt;
$$
\log \frac{\pi_\theta(y|x)}{\pi_{\text{ref}}(y|x)}.
$$&lt;p&gt;如果 chosen response 相对于 reference model 的提升幅度大于 rejected response，那么损失会降低；反之，损失会上升。&lt;/p&gt;
&lt;p&gt;这就是 DPO 和普通 SFT 的关键区别。SFT 只会提高目标答案的似然；DPO 同时使用 chosen 和 rejected，并且通过 reference model 保留了一个隐式 KL 约束。&lt;/p&gt;
&lt;h3 id="dpo-为什么能省掉-reward-model-和-ppo"&gt;DPO 为什么能省掉 reward model 和 PPO？
&lt;/h3&gt;&lt;p&gt;从工程角度看，DPO 的吸引力非常明显：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;不需要训练显式奖励模型；&lt;/li&gt;
&lt;li&gt;不需要在线 rollout；&lt;/li&gt;
&lt;li&gt;不需要 value model；&lt;/li&gt;
&lt;li&gt;训练形式接近普通监督学习，只需要偏好对数据；&lt;/li&gt;
&lt;li&gt;实现上主要依赖 chosen/rejected 的 log probability。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;因此，DPO 可以看作是把“奖励建模 + RL 优化”折叠成了一个 pairwise classification loss。它仍然在优化偏好，但不再显式执行强化学习过程。&lt;/p&gt;
&lt;p&gt;不过，DPO 的简化也带来了边界。&lt;/p&gt;
&lt;p&gt;首先，DPO 是离线方法。它依赖已有的偏好数据，如果数据覆盖不足，模型很难通过在线探索发现新的高质量行为。PPO 和 GRPO 可以在训练过程中不断采样当前模型的输出，再用奖励函数评价这些输出；DPO 通常不能做到这一点。&lt;/p&gt;
&lt;p&gt;其次，DPO 对偏好数据质量非常敏感。因为它直接把 chosen/rejected 对作为训练信号，如果偏好对噪声很大，或者 chosen 与 rejected 的差异并不稳定，模型会直接学习到这些偏差。&lt;/p&gt;
&lt;p&gt;第三，DPO 更适合对齐类任务和已有偏好数据充分的场景。对于数学、代码、复杂推理这类可以通过规则验证的任务，在线采样 + 规则奖励往往更有优势，因为模型可以生成大量候选解，再通过正确性信号筛选和强化。&lt;/p&gt;
&lt;p&gt;这也是为什么在推理模型训练中，GRPO 重新变得重要。&lt;/p&gt;
&lt;h2 id="simpodpo-的参考模型无关优化"&gt;SimPO：DPO 的参考模型无关优化
&lt;/h2&gt;&lt;p&gt;DPO 虽然已将 RLHF 折叠成一个优雅的离线分类损失，但它仍然依赖参考模型 $\pi_{\rm ref}$ 来实现隐式 KL 约束。这带来了两个实际问题：一是训练时需要同时加载策略模型和参考模型，显存与计算开销显著增加；二是 DPO 使用完整序列的 $\log \pi_\theta(y|x)$ 之和作为隐式奖励，由于每个 token 的 $\log p &amp;lt; 0$，更长的回答总 log probability 天然更低，模型容易通过生成冗长回答来“hack”优化目标（长度偏差）。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;SimPO（Simple Preference Optimization，简单偏好优化）&lt;/strong&gt; 正是针对这些痛点提出的 DPO 重要改进，由 Princeton NLP 团队在 2024 年提出（NeurIPS 2024）。它在保持 DPO 所有核心优势（无需奖励模型、无需在线采样、无需价值模型）的同时，进一步简化实现并显著提升性能。&lt;/p&gt;
&lt;h3 id="simpo-的核心设计"&gt;SimPO 的核心设计
&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;完全移除参考模型：不再需要 $\pi_{\rm ref}$，直接把当前策略模型 $\pi_\theta$ 的平均对数概率作为隐式奖励：
&lt;/p&gt;
$$
r(y) = \frac{1}{|y|} \log \pi_\theta(y \mid x)
$$&lt;p&gt;
其中 $|y|$ 是回答的 token 数量。这种长度归一化设计让奖励更贴合生成过程的 per-token 平均似然，从根本上缓解了长度偏差。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;引入目标奖励 margin：为了确保 chosen response 的奖励明显高于 rejected response，SimPO 在 sigmoid 内部加入一个可调的 margin 参数 $\gamma$：
&lt;/p&gt;
$$
\mathcal{L}_{\rm SimPO}(\theta)
= -\mathbb{E}_{(x,y_w,y_l)\sim \mathcal{D}}
\left[
\log \sigma \Bigl(
\beta \Bigl(
\frac{1}{|y_w|} \log \pi_\theta(y_w|x)
-
\frac{1}{|y_l|} \log \pi_\theta(y_l|x)
\Bigr)
- \gamma
\Bigr)
\right].
$$&lt;p&gt;
典型超参数范围：$\beta \in [2.0, 2.5]$（远大于 DPO 的 0.01–0.1），$\gamma \in [0.3, 1.6]$。&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;h3 id="simpo-的工程与性能优势"&gt;SimPO 的工程与性能优势
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;更轻量：无需参考模型，训练时间和 GPU 显存分别降低约 20% 和 10%，实现复杂度也大幅下降。&lt;/li&gt;
&lt;li&gt;性能更强：在 AlpacaEval 2 上比 DPO 最高提升 6.4 分，在 Arena-Hard 上最高提升 7.5 分，在 Llama-3、Mistral、Gemma-2 等多种基座上均有稳定领先。&lt;/li&gt;
&lt;li&gt;更少长度利用：生成回答长度与 SFT/DPO 模型接近，几乎不出现过度拉长现象。&lt;/li&gt;
&lt;li&gt;训练更稳定：长度归一化 + 显式 margin 让梯度信号更鲁棒，对偏好数据中的噪声有一定容忍度。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;SimPO 仍然是离线方法，完美继承 DPO 的所有工程便利性，但通过更精巧的隐式奖励设计（平均 log probability + margin），在去掉参考模型后反而获得了更好的对齐效果。这证明了：KL 正则化 RLHF 目标的解析形式还有进一步优化的空间。&lt;/p&gt;
&lt;h2 id="grpo保留在线-rl去掉价值模型"&gt;GRPO：保留在线 RL，去掉价值模型
&lt;/h2&gt;&lt;p&gt;GRPO（Group Relative Policy Optimization）可以看作是 PPO 的另一种简化方向。它没有像 DPO 那样完全放弃在线 RL，而是保留了“采样—打分—更新”的过程；但它去掉了 PPO 中最重的 critic / value model。&lt;/p&gt;
&lt;p&gt;它的核心问题是：如果 PPO 中的 value model 主要是为了提供 baseline、降低 advantage 估计的方差，那么是否可以不用额外训练一个 critic，而是直接从同一个 prompt 的一组回答中构造 baseline？&lt;/p&gt;
&lt;p&gt;GRPO 的答案是：可以。&lt;/p&gt;
&lt;h3 id="组内相对优势"&gt;组内相对优势
&lt;/h3&gt;&lt;p&gt;对于每个 prompt $x$，GRPO 不只采样一个回答，而是从当前策略中采样一组回答：&lt;/p&gt;
$$
\{y_1, y_2, \dots, y_G\} \sim \pi_{\theta_{\text{old}}}(\cdot|x).
$$&lt;p&gt;然后对每个回答计算奖励：&lt;/p&gt;
$$
\{r_1, r_2, \dots, r_G\}.
$$&lt;p&gt;奖励可以来自奖励模型，也可以来自规则函数。例如数学任务中可以根据最终答案是否正确给 reward；代码任务中可以根据测试用例是否通过给 reward。&lt;/p&gt;
&lt;p&gt;接下来，GRPO 用组内归一化的方式估计 advantage：&lt;/p&gt;
$$
A_i = \frac{r_i - \operatorname{mean}(r_1,\dots,r_G)}{\operatorname{std}(r_1,\dots,r_G)}.
$$&lt;p&gt;这个式子就是 GRPO 的核心。它不再问“这个 token 相对于 critic 预测值好多少”，而是问：&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;对于同一个 prompt，当前这条回答在同组候选答案中相对好多少？&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;因此，GRPO 的 baseline 是 prompt-specific 的组内平均奖励，而不是一个额外训练出来的 value function。&lt;/p&gt;
&lt;h3 id="grpo-的目标函数"&gt;GRPO 的目标函数
&lt;/h3&gt;&lt;p&gt;GRPO 仍然沿用了 PPO 的 clipped objective。对于第 $i$ 个回答中的第 $t$ 个 token，定义概率比率：&lt;/p&gt;
$$
\rho_{i,t}(\theta) = \frac{\pi_\theta(y_{i,t} \mid x, y_{i,\lt t})}{\pi_{\theta_{\text{old}}}(y_{i,t} \mid x, y_{i,\lt t})}.
$$&lt;p&gt;GRPO 的策略目标可以写成：&lt;/p&gt;
$$
\begin{aligned}
\mathcal{L}_{\text{GRPO}}(\theta)
&amp;= \mathbb{E}_{i,t} \Bigl[
\min\Bigl(
\rho_{i,t}(\theta) A_i,\
\operatorname{clip}\bigl(\rho_{i,t}(\theta),1-\epsilon,1+\epsilon\bigr) A_i
\Bigr)
\Bigr] \\
&amp;\quad - \beta \, \mathbb{D}_{\mathrm{KL}}\bigl(\pi_\theta \,\|\, \pi_{\text{ref}}\bigr).
\end{aligned}
$$&lt;p&gt;这里的 $A_i$ 是 sequence-level advantage，但会被应用到该回答的 token-level log probability 上。也就是说，如果某个回答在组内 reward 更高，那么这条回答中所有 token 的生成概率整体上都会被提升；如果 reward 更低，则对应 token 概率会被压低。&lt;/p&gt;
&lt;h3 id="grpo-相比-ppo-简化在哪里"&gt;GRPO 相比 PPO 简化在哪里？
&lt;/h3&gt;&lt;p&gt;GRPO 最大的变化是去掉了 value model。&lt;/p&gt;
&lt;p&gt;在 PPO 中，我们需要训练 $V_\psi(s_t)$ 来估计每个状态的期望回报；在 GRPO 中，同一 prompt 下的多个回答已经提供了一个局部比较集合，因此可以用组内均值作为 baseline，用组内标准差做归一化。&lt;/p&gt;
&lt;p&gt;这带来几个直接好处：&lt;/p&gt;
&lt;p&gt;第一，显存和计算成本更低。去掉 critic 以后，不需要再维护一个额外的价值网络，也不需要训练 value loss。&lt;/p&gt;
&lt;p&gt;第二，训练信号更适合可验证任务。对于数学、代码这类任务，reward 往往可以由规则直接给出。此时最自然的问题不是“这个 token 的价值是多少”，而是“同一个题目下，哪些完整回答是正确的”。GRPO 的组内比较正好贴合这一点。&lt;/p&gt;
&lt;p&gt;第三，GRPO 仍然保留在线探索能力。与 DPO 不同，GRPO 可以不断从当前策略采样新回答，然后用规则或奖励模型打分。这对于推理能力的自我提升非常重要。&lt;/p&gt;
&lt;p&gt;当然，GRPO 也不是没有代价。它需要对每个 prompt 采样多个回答，因此 rollout 成本会增加；如果组大小太小，组内 baseline 的估计会不稳定；如果 reward 过于稀疏，比如一组回答全错或全对，advantage 信号也会变弱。&lt;/p&gt;
&lt;h2 id="dapogrpo-的工程精细化"&gt;DAPO：GRPO 的工程精细化
&lt;/h2&gt;&lt;p&gt;DAPO 保留了 GRPO 的整体框架（组采样 + 组内归一化优势 + clipped surrogate），但在四个关键环节做了针对性调整，使训练信号更有效、探索更充分、长序列处理更合理。&lt;/p&gt;
&lt;h3 id="clip-higher非对称裁剪"&gt;Clip-Higher：非对称裁剪
&lt;/h3&gt;&lt;p&gt;GRPO 使用对称裁剪区间 $[1-\varepsilon, 1+\varepsilon]$。当优势 $A_i &amp;gt; 0$ 时，概率比率 $\rho_{i,t}(\theta)$ 一旦超过 $1+\varepsilon$ 即被裁剪。这对旧策略下概率较低的 token 尤其不利：即使新策略大幅提升了该 token 的概率，也会被硬性截断，导致有用探索信号丢失。&lt;/p&gt;
&lt;p&gt;DAPO 将裁剪解耦为非对称形式：&lt;/p&gt;
$$
\text{clip}\bigl(\rho_{i,t}(\theta), 1-\varepsilon_{\rm low}, 1+\varepsilon_{\rm high}\bigr)
$$&lt;p&gt;其中 $\varepsilon_{\rm high} &amp;gt; \varepsilon_{\rm low}$。&lt;strong&gt;仅放宽正方向的上界&lt;/strong&gt;，允许低概率 token 有更大提升空间，同时对负方向仍保持严格约束。这既保留了 PPO/GRPO 的稳定性，又显著增强了探索能力，避免策略过早退化为确定性输出。&lt;/p&gt;
&lt;h3 id="动态采样保证有效优势信号"&gt;动态采样：保证有效优势信号
&lt;/h3&gt;&lt;p&gt;GRPO 在某些 prompt 下很容易采出全对或全错的一组回答，此时组内均值与标准差相等，$A_i = 0$，整组梯度为零，采样算力被白白浪费。&lt;/p&gt;
&lt;p&gt;DAPO 增加了一个采样约束：&lt;strong&gt;必须保证组内存在至少一个正确和一个错误的回答&lt;/strong&gt;（即 advantage 不全为零）。若当前采样不满足条件，则重新对该 prompt 进行 rollout，直到满足为止。这种“动态重采样”确保每一批数据都能产生有意义的梯度信号，从根本上提高了采样效率。&lt;/p&gt;
&lt;h3 id="token-level-梯度聚合"&gt;Token-level 梯度聚合
&lt;/h3&gt;&lt;p&gt;GRPO 的目标函数对每条回答先做 token 平均，再跨回答平均：&lt;/p&gt;
$$
\frac{1}{G}\sum_{i=1}^G\frac{1}{|y_i|}\sum_{t=1}^{|y_i|}\cdots
$$&lt;p&gt;这导致长回答对整体 loss 的贡献被严重低估：200 token 的回答权重只有 10 token 回答的 1/20。即使长回答中包含关键推理步骤，其梯度也会被稀释。&lt;/p&gt;
&lt;p&gt;DAPO 改为&lt;strong&gt;全局 token 级聚合&lt;/strong&gt;：&lt;/p&gt;
$$
\frac{1}{\sum_{i=1}^G |y_i|}\sum_{i=1}^G\sum_{t=1}^{|y_i|}\min\bigl(\rho_{i,t}(\theta)A_i,\ \text{clip}(\cdots)A_i\bigr)
$$&lt;p&gt;每条 token 对梯度的贡献完全平等，与回答长度无关。这让长 CoT 中真正重要的 token 获得应有权重，同时有效抑制冗长废话的负面影响。&lt;/p&gt;
&lt;h3 id="超长回答奖励塑形"&gt;超长回答奖励塑形
&lt;/h3&gt;&lt;p&gt;长回答容易“hack”奖励：正确但啰嗦的回答拿高分，错误但重复的回答也可能因为长度而被误判。DAPO 引入软惩罚机制：当回答长度超过第一阈值后，奖励线性衰减；超过第二阈值后直接视为无效。这让奖励信号更干净，引导模型学会“简洁且正确”。&lt;/p&gt;
&lt;p&gt;综合以上四点，DAPO 的完整目标可写为：&lt;/p&gt;
$$
\begin{aligned}
\mathcal{L}_{\rm DAPO}(\theta)
&amp;= \mathbb{E}_{x\sim\mathcal{D},\ \{y_i\}\sim\pi_{\theta_{\rm old}}}\Bigg[
\frac{1}{\sum_i |y_i|}\sum_i\sum_t
\min\Bigl(
\rho_{i,t}(\theta)A_i,\
\text{clip}\bigl(\rho_{i,t}(\theta),1-\varepsilon_{\rm low},1+\varepsilon_{\rm high}\bigr)A_i
\Bigr)
\Bigg] \\
&amp;\quad - \beta\,\mathbb{D}_{\rm KL}(\pi_\theta\|\pi_{\rm ref})
\end{aligned}
$$&lt;p&gt;（其中动态采样约束隐含在期望的采样过程中）&lt;/p&gt;
&lt;p&gt;DAPO 的本质是&lt;strong&gt;在不改变 GRPO 核心思想的前提下，通过更精细的信号传递和约束，让在线 RL 在长序列场景下真正稳定可用&lt;/strong&gt;。&lt;/p&gt;
&lt;h2 id="gspo从-token-级到-sequence-级的优化粒度转变"&gt;GSPO：从 token 级到 sequence 级的优化粒度转变
&lt;/h2&gt;&lt;p&gt;GSPO 进行了一次更本质的调整：把 importance sampling 从 token 级提升到 sequence 级。&lt;/p&gt;
&lt;h3 id="为什么-token-级在某些架构下存在问题"&gt;为什么 token 级在某些架构下存在问题？
&lt;/h3&gt;&lt;p&gt;在 MoE 模型中，每个 token 的专家激活是动态的。旧策略和新策略在生成同一回答时，不同 token 可能激活完全不同的专家子网络，导致 token 级的比率 $\rho_{i,t}(\theta)$ 波动极大：有的 token ratio 飙升数百倍，有的接近零。这不仅使 clipping 产生大量无效梯度，还引入了与奖励（sequence-level）不匹配的结构性噪声，训练极不稳定。&lt;/p&gt;
&lt;h3 id="gspo-的核心改动"&gt;GSPO 的核心改动
&lt;/h3&gt;&lt;p&gt;GSPO 用整条回答的 sequence-level 概率比替代 token-level 比率：&lt;/p&gt;
$$
s_i(\theta) = \left( \frac{\pi_\theta(y_i|x)}{\pi_{\theta_{\rm old}}(y_i|x)} \right)^{1/|y_i|}
= \exp\left( \frac{1}{|y_i|}\sum_{t=1}^{|y_i|}\log\rho_{i,t}(\theta) \right)
$$&lt;p&gt;即整条回答概率比的长度归一化几何平均。然后在 clipped objective 中，同一回答的所有 token 都使用同一个 $s_i(\theta)$：&lt;/p&gt;
$$
\begin{aligned}
\mathcal{L}_{\rm GSPO}(\theta)
&amp;= \mathbb{E}_{x\sim\mathcal{D},\ \{y_i\}\sim\pi_{\theta_{\rm old}}}\Bigg[
\frac{1}{G}\sum_i\frac{1}{|y_i|}\sum_t
\min\Bigl(
s_i(\theta)A_i,\
\text{clip}\bigl(s_i(\theta),1-\varepsilon,1+\varepsilon\bigr)A_i
\Bigr)
\Bigg] \\
&amp;\quad - \beta\,\mathbb{D}_{\rm KL}(\pi_\theta\|\pi_{\rm ref})
\end{aligned}
$$&lt;h3 id="优势"&gt;优势
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;同一回答内所有 token 受到完全一致的更新强度，消除了 token 间剧烈波动带来的高方差；&lt;/li&gt;
&lt;li&gt;与 sequence-level 奖励天然对齐，credit assignment 更干净；&lt;/li&gt;
&lt;li&gt;特别适合 MoE 架构，因为不再依赖每个 token 的专家激活路径，训练稳定性显著提升。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;GSPO 的代价是牺牲了一部分 token 级的精细控制，但对最终奖励只依赖完整回答正确性的任务来说，这种 sequence-level 视角反而更合理。&lt;/p&gt;
&lt;p&gt;DAPO 和 GSPO 可以正交组合使用：DAPO 负责解决采样效率、梯度稀释和探索问题，GSPO 负责解决架构带来的方差问题。两者都保留了 GRPO “去掉价值模型 + 组内相对优势 + 在线采样” 的核心优势，只是把信号传递和优化粒度做得更贴合实际训练场景。&lt;/p&gt;</description></item><item><title>详解PPO与GRPO</title><link>https://qychen2001.github.io/blog/p/%E8%AF%A6%E8%A7%A3ppo%E4%B8%8Egrpo/</link><pubDate>Fri, 28 Mar 2025 00:00:00 +0000</pubDate><guid>https://qychen2001.github.io/blog/p/%E8%AF%A6%E8%A7%A3ppo%E4%B8%8Egrpo/</guid><description>&lt;blockquote&gt;
&lt;p&gt;本文是&lt;a class="link" href="https://yugeten.github.io/posts/2025/01/ppogrpo/" target="_blank" rel="noopener"
&gt;《A vision researcher’s guide to some RL stuff: PPO &amp;amp; GRPO》&lt;/a&gt;的中文翻译，并添加了必要的注释与修订，使得更容易理解。这篇博客是我认为把PPO讲的最清楚的博客了。此外，还会添加部分&lt;a class="link" href="https://huggingface.co/blog/NormalUhr/grpo" target="_blank" rel="noopener"
&gt;《DeepSeek-R1 Dissection: Understanding PPO &amp;amp; GRPO Without Any Prior Reinforcement Learning Knowledge》&lt;/a&gt;的内容，以辅助理解。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id="llm的预训练与后训练"&gt;LLM的预训练与后训练
&lt;/h2&gt;&lt;p&gt;LLM的训练可以分为预训练和后训练两个阶段：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;预训练：这是经典的“向模型投入数据”阶段，模型通过大规模网络数据进行训练，以进行下一个词预测；&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;后训练：这是我们尝试提高模型推理能力的阶段。通常后训练分为两个阶段，即：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;阶段1：SFT（监督微调）：&lt;/strong&gt; 顾名思义，我们首先通过在少量高质量专家推理数据上微调LLM来进行监督学习；可以理解为指令跟随、问答和/或思维链。希望到这个训练阶段结束时，模型已经学会了如何模仿专家。如果我们有无限量的高质量专家数据，这显然是理想的学习方式，可惜并没有。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;阶段2：RLHF（从人类反馈中进行强化学习）：&lt;/strong&gt; 人类专家推理数据不足？这就是强化学习大放异彩的地方。RLHF 利用人类反馈来训练奖励模型，然后通过强化学习引导大语言模型的学习。这种方法使模型与人类复杂的偏好保持一致。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;h2 id="deepseek的超高效后训练"&gt;DeepSeek的超高效后训练
&lt;/h2&gt;&lt;p&gt;值得注意的是，DeepSeek R1技术报告中一个最令人惊讶的地方是，他们的R1-zero模型完全跳过了SFT部分，直接在基础模型（DeepSeek V3）上应用RL。这样做有几个好处：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;计算效率：&lt;/strong&gt; 跳过后训练的一个阶段带来了计算效率；&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;开放式学习：&lt;/strong&gt; 允许模型通过探索“自我进化”推理能力；&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;对齐：&lt;/strong&gt; 避免了由人工收集的SFT数据引入的偏见。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;注意：&lt;/strong&gt; 虽然看到有人通过跳过后训练的一个阶段来节省计算资源似乎是一个“显而易见”的想法，但我怀疑如果没有一个非常好的基础模型，你无法做到这一点。&lt;/p&gt;
&lt;p&gt;但他们并没有止步于此！DeepSeek还通过引入GRPO来替代PPO，使RLHF部分更加高效，这消除了对单独的价值函数（通常与策略模型大小相当）的需求，将内存和计算开销减少了约50%。为了了解他们为什么以及如何做到这一点，并满足我们自己的求知欲，现在让我们来看看RLHF的确切做法以及这些算法的作用。&lt;/p&gt;
&lt;h2 id="rlhf"&gt;RLHF
&lt;/h2&gt;&lt;p&gt;让我们将RLHF的工作流程分解为步骤：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;步骤1&lt;/strong&gt;：对于每个提示，从模型中采样多个响应；&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;步骤2&lt;/strong&gt;：人类根据质量对这些输出进行排序；&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;步骤3&lt;/strong&gt;：训练一个&lt;strong&gt;奖励模型&lt;/strong&gt;，以在给定任何模型响应时，预测人类偏好/排序；&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;步骤4&lt;/strong&gt;：使用&lt;strong&gt;RL（例如PPO、GRPO）&lt;/strong&gt; 来微调模型，以最大化奖励模型的分数。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;正如我们所见，这个过程相对简单，有两个可学习的组件，即&lt;strong&gt;奖励模型&lt;/strong&gt;和&lt;strong&gt;RL&lt;/strong&gt;。现在让我们更详细地探讨每个组件。&lt;/p&gt;
&lt;h2 id="奖励模型"&gt;奖励模型
&lt;/h2&gt;&lt;p&gt;奖励模型实际上是自动化工作的最重要组成部分：现实情况是，我们不可能让人类对模型的所有输出进行排序。一种节省成本的方法是让标注员对一小部分LLM输出进行评分，然后&lt;strong&gt;训练一个模型来预测这些标注员的偏好&lt;/strong&gt;——这就是奖励模型的作用。&lt;/p&gt;
&lt;p&gt;具体来说，我们用$R_\phi$表示可学习的奖励模型。给定一个提示$p$，LLM生成$N$个响应${r_1, r_2,&amp;hellip;r_N}$。然后根据人类评分者的偏好，响应$r_i$优于$r_j$，奖励模型通过最小化以下目标函数进行训练：&lt;/p&gt;
$$
\mathcal{L}(\phi) = -\log \sigma(R_\phi(p, r_i) - R_\phi(p, r_j)),
$$&lt;p&gt;
其中$\sigma$表示sigmoid函数。&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;旁注&lt;/strong&gt;：该目标函数源自&lt;strong&gt;Bradley-Terry模型&lt;/strong&gt;，该模型定义了评分者偏好$r_i$而非$r_j$的概率为：&lt;/p&gt;
$$
P(r_i \succ r_j) = \frac{\exp\big(R_\phi(p, r_i)\big)}{\exp\big(R_\phi(p, r_i)\big) + \exp\big(R_\phi(p, r_j)\big)}.$$&lt;p&gt;对该概率取负对数似然得到上述损失函数$\mathcal{L}(\phi)$。sigmoid函数$\sigma$可以对Bradley-Terry整理后得到。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id="ppo"&gt;PPO
&lt;/h2&gt;&lt;blockquote&gt;
&lt;p&gt;这部分内容是为那些对PPO感到好奇的读者准备的，如果你的目标是理解GRPO，那么你其实不需要理解这部分内容。我只能说，虽然我终于理解PPO的工作原理让我感到非常高兴，但当我意识到GRPO比PPO简单得多时，我感到一种极大的解脱感。所以，如果你准备好体验一次情感的过山车——让我们深入探讨吧。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;首先，一个高层次的概述。PPO代表近端策略优化，它需要以下组件：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;策略模型 ($\pi_\theta$)&lt;/strong&gt;：经过预训练或SFT的LLM；&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;奖励模型 ($R_\phi$)&lt;/strong&gt;：一个经过训练并冻结的网络，它为给定提示的&lt;strong&gt;完整响应&lt;/strong&gt;提供标量奖励；&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;价值函数 ($V_\gamma$)&lt;/strong&gt;：它是一个可学习的网络，接受给定提示的&lt;strong&gt;部分响应&lt;/strong&gt;并预测标量奖励。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;一旦我们了解了工作流程，每个组件的作用就会变得更加清晰，该流程包含五个阶段：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;生成响应&lt;/strong&gt;：LLM为给定提示生成多个响应；&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;评分响应&lt;/strong&gt;：奖励模型为每个响应分配奖励；&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;计算优势&lt;/strong&gt;：使用GAE（广义优势估计）计算优势（稍后会详细介绍，它用于训练LLM）；&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;优化策略&lt;/strong&gt;：通过优化总目标来更新LLM；&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;更新价值函数&lt;/strong&gt;：训练价值函数，使其更好地预测给定部分响应的奖励。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;现在让我们详细看看这些阶段/组件，然后看看它们是如何结合在一起的。&lt;/p&gt;
&lt;h3 id="术语状态和动作"&gt;术语：状态和动作
&lt;/h3&gt;&lt;p&gt;在我们继续之前，再介绍一些强化学习的术语。在本节的讨论中，我们将使用术语&lt;strong&gt;状态&lt;/strong&gt;，记为$s_t$，以及&lt;strong&gt;动作&lt;/strong&gt;，记为$a_t$。需要注意的是，这里的下标$t$用于表示&lt;strong&gt;token级别&lt;/strong&gt;的状态和动作；相比之下，之前我们在定义提示$p$和响应$r_i$时，下标$i$用于表示&lt;strong&gt;实例级别&lt;/strong&gt;的响应。&lt;/p&gt;
&lt;p&gt;为了更清楚地说明这一点，假设我们给LLM一个提示$p$。然后LLM开始逐个token地生成一个长度为$T$的响应$r_i$：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;$t=0$：我们的状态只是提示，即$s_0 = {p}$，第一个动作$a_0$是LLM生成的第一个词token；&lt;/li&gt;
&lt;li&gt;$t=1$：状态变为$s_1 = {p, a_0}$，因为LLM在基于状态生成下一个动作$a_1$；
&amp;hellip;&lt;/li&gt;
&lt;li&gt;$t=T-1$：状态为$s_{T-1} = {p, a_{0:T-2}}$，LLM生成最后一个动作$a_{T-1}$。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;所有动作串联在一起形成一个响应，即$r_i = {a_0, a_1,&amp;hellip;a_{T-1}}$。&lt;/p&gt;
&lt;h3 id="广义优势估计-gae"&gt;广义优势估计 (GAE)
&lt;/h3&gt;&lt;p&gt;我们的策略更新是为了优化&lt;strong&gt;优势&lt;/strong&gt;——直观地说，它定义了在状态 $s_t$（即提示 + 到目前为止生成的词）下，&lt;strong&gt;特定动作&lt;/strong&gt; $a_t$（即词）相对于策略将采取的&lt;strong&gt;平均动作&lt;/strong&gt;的优越程度。形式上：&lt;/p&gt;
$$
A_t = Q(s_t, a_t) - V(s_t)
$$&lt;p&gt;其中 $Q(s_t, a_t)$ 是在状态 $s_t$ 下采取特定动作 $a_t$ 的预期累积奖励，而 $V(s_t)$ 是策略在状态 $s_t$ 下采取平均动作的预期累积奖励。&lt;/p&gt;
&lt;p&gt;有两种主要的方法来估计这种优势，每种方法都有其权衡，即：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;蒙特卡洛 (MC)&lt;/strong&gt;：使用完整轨迹的奖励（即完整响应）。这种方法由于稀疏奖励而具有高方差——从LLM中获取足够多的样本以使用MC进行优化是昂贵的，但它确实具有低偏差，因为我们可以准确地建模奖励；&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;时序差分 (TD)&lt;/strong&gt;：使用一步轨迹奖励（即测量在给定提示下刚刚生成的词的好坏）。通过这样做，我们可以在token级别上计算奖励，这显著降低了方差，但与此同时，偏差上升了，因为我们无法像准确预测部分生成的响应的最终奖励那样准确。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;这就是GAE的用武之地——它被提出来&lt;strong&gt;通过多步TD来平衡偏差和方差&lt;/strong&gt;。然而，回想一下我们之前提到的，如果响应不完整，奖励模型将返回0：我们如何在不知道生成一个词之前和之后的奖励变化的情况下计算TD？因此，我们引入了一个模型来完成这项工作，我们称之为“价值函数”。&lt;/p&gt;
&lt;h4 id="为什么需要gae"&gt;为什么需要GAE
&lt;/h4&gt;&lt;p&gt;为了使这些概念更加直观，让我们打个比方：把RL训练过程想象成一个小学生考试的场景。我们（正在训练的模型）就像学生，努力争取高分，给我们打分的老师就像奖励模型，而我们根据成绩获得零花钱的父母则类似于价值函数。接下来，我们将逐步探讨为什么仅靠最终分数是不够的，也就是为什么需要GAE。&lt;/p&gt;
&lt;p&gt;假设我和我的弟弟在同一所小学的同一个班级。老师给我们的考试打分，给出“绝对分数”。我通常得分在80分以上，而我的弟弟经常得30分左右。然后我们直接把这些分数拿给爸爸，要求零花钱——这意味着我们的“奖励”就是我们的原始考试分数。谁的分数高，谁就能得到更多的零花钱。&lt;/p&gt;
&lt;p&gt;乍一看，这似乎挺好。但很快就会出现两个大问题：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;不公平：&lt;/strong&gt; 如果我弟弟通过大量努力从30分提高到60分，他仍然远远不及我通常的80分以上。他没有得到应有的鼓励。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;不稳定：&lt;/strong&gt; 我自己追求更高的分数可能会导致极端的学习方法（例如，不分昼夜地死记硬背，熬夜到很晚）。有时我可能得95分，其他时候只有60分，因此我的分数以及由此产生的奖励信号会剧烈波动。&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;结果是，使用绝对分数作为奖励会导致奖励的巨大波动，而我弟弟最终会觉得不值得通过小步改善来提高。&lt;/p&gt;
&lt;p&gt;在强化学习中，如果我们简单地只优化最终奖励，我们可能会遇到高方差和对部分改进激励不足的问题。换句话说，策略缺乏一个与其当前水平相匹配的基线，这阻碍了训练效率。&lt;/p&gt;
&lt;p&gt;认识到这个问题后，爸爸意识到“这不只是关于绝对分数，而是关于你相对于当前水平的进步有多大。”&lt;/p&gt;
&lt;p&gt;所以他决定：
将我的“预测分数线”设为80分，弟弟的设为40分。如果我们考试超过这些分数线，我们就能得到更多的零花钱；如果没有，我们得到的就很少甚至没有。&lt;/p&gt;
&lt;p&gt;因此，如果弟弟努力学习，从30分跃升到60分，他就比他的“预测分数线”高出20分，这意味着丰厚的奖励。与此同时，如果我仍然保持在80分左右，增量的收益较小，所以我未必会比他得到更多。这种安排鼓励每个人从自己的基线开始进步，而不是单纯比较绝对分数。&lt;/p&gt;
&lt;h4 id="价值函数"&gt;价值函数
&lt;/h4&gt;&lt;p&gt;价值函数经过训练，能够&lt;strong&gt;仅根据部分状态预测最终奖励&lt;/strong&gt;，从而我们可以计算TD。训练价值函数 $V_\gamma$ 相当简单：&lt;/p&gt;
&lt;p&gt;给定一个部分状态 $s_t$，我们希望预测在完整状态 $s_T = {p, r}$ 下奖励模型的输出。价值函数的目标函数可以写为&lt;/p&gt;
$$
L(\gamma) = \mathbb{E}_t \left[(V_\gamma(s_t) - \text{sg}(R_\phi(s_T)))^2\right],
$$&lt;p&gt;其中 $\text{sg}$ 表示停止梯度操作。正如我们所见，价值函数通过简单的L2损失与奖励模型的分数进行训练。&lt;/p&gt;
&lt;p&gt;你可能会注意到，尽管奖励模型 $R_\phi$ 在PPO之前已经训练并冻结，价值函数却与LLM一起训练，尽管它的任务也只是预测奖励。这是因为价值函数必须根据&lt;strong&gt;当前策略&lt;/strong&gt;估计部分响应的奖励；因此，它必须与LLM一起更新，以避免其预测变得过时和错位。&lt;/p&gt;
&lt;p&gt;直觉上的理解就是：分数线不会一成不变，他需要随着我们的进步不断“重新调整”。如果弟弟的水平提升到60分左右，那么40分的基线就不再公平。同样，如果我稳定在85分左右，爸爸可能也需要调整我的分数线。换句话说，爸爸也需要学习，特别是关于我和弟弟进步的速度。&lt;/p&gt;
&lt;h4 id="回到gae"&gt;回到GAE
&lt;/h4&gt;&lt;p&gt;有了价值函数 $V_\gamma$，我们现在有了一种方法来预测部分状态的奖励。现在让我们继续讨论GAE，它正如之前提到的，计算多步TD目标：&lt;/p&gt;
$$
A^{\text{GAE}}_K = \delta_0 + \lambda \delta_1 + \lambda^2 \delta_2 ... + (\lambda)^{K-1} \delta_{K-1} = \sum^{K-1}_{t=0} (\lambda)^t \delta_t,
$$&lt;p&gt;其中 $K$ 表示TD步数，且 $K&amp;lt;T$（因为显然你不能计算超出轨迹长度的TD）。$\delta_t$ 表示第 $t$ 步的TD误差，计算公式为：&lt;/p&gt;
$$
\delta_t = V_\gamma(s_{t+1}) - V_\gamma(s_t)
$$&lt;p&gt;简而言之，TD误差计算了一个时间步的预期总奖励之间的差异，而 $A_{K}^{\text{GAE}}$ 通过计算 $K$ 步内的单步TD误差的聚合来估计优势。GAE公式中的 $\lambda$ 控制了方差和偏差之间的权衡：当 $\lambda =0$ 时，GAE简化为单步TD；当 $\lambda=1$ 时，GAE变为MC。&lt;/p&gt;
&lt;p&gt;在RLHF中，我们希望最大化这个优势项，从而最大化LLM生成的每个token的奖励。&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;旁注&lt;/strong&gt;：为了简化，我在这里省略了一些细节。原本在GAE中还有一个折扣因子 $\eta$：&lt;/p&gt;
$$A^{\text{GAE}}_K = \sum^{K-1}_{t=0} (\lambda\eta)^t \delta_t,$$&lt;p&gt;它也用于TD误差 $\delta_t$，并且还有一个额外的奖励项&lt;/p&gt;
$$\delta_t = R_\phi(s_t) + \eta V_\gamma(s_{t+1}) - V_\gamma(s_t).$$&lt;p&gt;
但由于我们几乎总是有 $\eta=1$，并且对于 $t&amp;lt;T$ 的情况 $R_\phi(s_t)=0$，我为了简化而省略了这些项。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3 id="ppo目标函数"&gt;PPO目标函数
&lt;/h3&gt;&lt;p&gt;PPO目标函数有几个组成部分，即1）剪裁的替代目标，2）熵奖励，3）KL惩罚。&lt;/p&gt;
&lt;h4 id="1-剪裁的替代目标"&gt;1. 剪裁的替代目标
&lt;/h4&gt;&lt;p&gt;这是我们最大化$A_K^{\text{GAE}}$的地方，因此LLM预测的每个token都能最大化奖励（或者根据之前的优势定义，LLM预测的每个token都应该比其平均预测好得多）。剪裁的替代目标通过概率比率$c_t(\pi_\theta)$来约束策略更新：&lt;/p&gt;
$$
L^{\text{clip}}(\theta) = \mathbb{E}_t \left[ \min(c_t(\pi_\theta)A^{GAE}_t, \text{clip}(c_t(\pi_\theta),1-\epsilon, 1+\epsilon)A^{GAE}_t)\right],
$$&lt;p&gt;其中$\epsilon$控制剪裁范围，$c_t(\pi_\theta)$是在给定累积状态$s_t$下，预测特定token $a_t$的概率比率，更新前后分别为：&lt;/p&gt;
$$
c_t(\pi_\theta) = \frac{\pi_\theta (a_t | s_t)}{\pi_{\theta_{\text{old}}} (a_t | s_t)}.
$$&lt;p&gt;直觉上的理解就是：即使有了“分数线”，新的问题也可能出现。例如：如果我在一次考试中突然突破并取得95或100分，爸爸可能会给我丰厚的奖励，促使我在下次考试前采取过于激进的学习模式。我的成绩可能会在极端之间波动（95分和60分），导致巨大的奖励波动。&lt;/p&gt;
&lt;p&gt;因此，爸爸决定适度调整我在每一步可以更新学习策略的幅度。他不会因为一次好成绩就给我成倍增加零花钱。如果他给得太多，我可能会走向极端的探索；如果给得太少，我又会失去动力。所以他必须找到一个平衡点。&lt;/p&gt;
&lt;p&gt;简单来说，得到 100 分会获得额外奖励，但爸爸设定了一个“天花板”，以防我过于放纵。他会在下次考试中重新评估，采取稳定的方法而不是加剧极端波动。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;具体示例&lt;/strong&gt;:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;假设LLM为单词&lt;code&gt;unlimited&lt;/code&gt;分配了以下概率：
&lt;ul&gt;
&lt;li&gt;&lt;em&gt;&lt;strong&gt;更新前&lt;/strong&gt;&lt;/em&gt;：0.1，&lt;/li&gt;
&lt;li&gt;&lt;em&gt;&lt;strong&gt;更新后&lt;/strong&gt;&lt;/em&gt;：0.3。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;那么概率比率 $c_t=0.3/0.1=3$；&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;如果我们取 $\epsilon=0.2$，$c_t$ 被截断为1.2；&lt;/li&gt;
&lt;li&gt;最终的截断代理损失为 $L^{\text{clip}}(\pi_\theta) = 1.2A_K^{\text{GAE}}$。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;你可以将截断视为防止过度自信的一种方法。如果没有截断，一个较大的 $A_K^{\text{GAE}}$ 可能会导致策略过度承诺于某个动作。&lt;/p&gt;
&lt;h4 id="2-kl散度惩罚"&gt;2. KL散度惩罚
&lt;/h4&gt;&lt;p&gt;此外，我们还有KL散度惩罚，它防止当前策略$\theta$与我们在微调时所基于的原始模型$\theta_{\text{orig}}$偏离太远：
&lt;/p&gt;
$$
\text{KL}(\theta) = \mathbb{E}_{s_t} \left[ \mathbb{D}_{\text{KL}}(\pi_{\theta\text{orig}}(\cdot | s_t) || \pi_{\theta}(\cdot | s_t)) \right]
$$&lt;p&gt;KL散度通过在序列和批次上取平均来简单估计。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;伪代码:&lt;/strong&gt;&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt; 1
&lt;/span&gt;&lt;span class="lnt"&gt; 2
&lt;/span&gt;&lt;span class="lnt"&gt; 3
&lt;/span&gt;&lt;span class="lnt"&gt; 4
&lt;/span&gt;&lt;span class="lnt"&gt; 5
&lt;/span&gt;&lt;span class="lnt"&gt; 6
&lt;/span&gt;&lt;span class="lnt"&gt; 7
&lt;/span&gt;&lt;span class="lnt"&gt; 8
&lt;/span&gt;&lt;span class="lnt"&gt; 9
&lt;/span&gt;&lt;span class="lnt"&gt;10
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-python" data-lang="python"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="c1"&gt;# 计算原始策略和当前策略/模型之间的KL散度&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="n"&gt;logits_orig&lt;/span&gt; &lt;span class="o"&gt;=&lt;/span&gt; &lt;span class="n"&gt;original_model&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;states&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt; &lt;span class="c1"&gt;# 原始模型的logits&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="n"&gt;logits_current&lt;/span&gt; &lt;span class="o"&gt;=&lt;/span&gt; &lt;span class="n"&gt;current_model&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;states&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt; &lt;span class="c1"&gt;# 当前模型的logits&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="n"&gt;probs_orig&lt;/span&gt; &lt;span class="o"&gt;=&lt;/span&gt; &lt;span class="n"&gt;F&lt;/span&gt;&lt;span class="o"&gt;.&lt;/span&gt;&lt;span class="n"&gt;softmax&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;logits_orig&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt; &lt;span class="n"&gt;dim&lt;/span&gt;&lt;span class="o"&gt;=-&lt;/span&gt;&lt;span class="mi"&gt;1&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="n"&gt;log_probs_orig&lt;/span&gt; &lt;span class="o"&gt;=&lt;/span&gt; &lt;span class="n"&gt;F&lt;/span&gt;&lt;span class="o"&gt;.&lt;/span&gt;&lt;span class="n"&gt;log_softmax&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;logits_orig&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt; &lt;span class="n"&gt;dim&lt;/span&gt;&lt;span class="o"&gt;=-&lt;/span&gt;&lt;span class="mi"&gt;1&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="n"&gt;log_probs_current&lt;/span&gt; &lt;span class="o"&gt;=&lt;/span&gt; &lt;span class="n"&gt;F&lt;/span&gt;&lt;span class="o"&gt;.&lt;/span&gt;&lt;span class="n"&gt;log_softmax&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;logits_current&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt; &lt;span class="n"&gt;dim&lt;/span&gt;&lt;span class="o"&gt;=-&lt;/span&gt;&lt;span class="mi"&gt;1&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="n"&gt;kl_div&lt;/span&gt; &lt;span class="o"&gt;=&lt;/span&gt; &lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;probs_orig&lt;/span&gt; &lt;span class="o"&gt;*&lt;/span&gt; &lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;log_probs_orig&lt;/span&gt; &lt;span class="o"&gt;-&lt;/span&gt; &lt;span class="n"&gt;log_probs_current&lt;/span&gt;&lt;span class="p"&gt;))&lt;/span&gt;&lt;span class="o"&gt;.&lt;/span&gt;&lt;span class="n"&gt;sum&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;dim&lt;/span&gt;&lt;span class="o"&gt;=-&lt;/span&gt;&lt;span class="mi"&gt;1&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="n"&gt;kl_penalty&lt;/span&gt; &lt;span class="o"&gt;=&lt;/span&gt; &lt;span class="n"&gt;kl_div&lt;/span&gt;&lt;span class="o"&gt;.&lt;/span&gt;&lt;span class="n"&gt;mean&lt;/span&gt;&lt;span class="p"&gt;()&lt;/span&gt; &lt;span class="c1"&gt;# 在序列和批次上取平均&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;p&gt;直觉上的理解就是：如果我只专注于高分，我可能会采取一些可疑的手段，比如作弊或威胁老师给我满分。显然，这违反了所有规则。在LLM中，类似的情况是生成有害或伪造的内容以人为地提升某些奖励指标。&lt;/p&gt;
&lt;p&gt;因此，爸爸还设定了一个额外的规则：无论如何，你都不能偏离你最初、诚实的学习方法太远。如果你偏离了基线太多，即使分数很高，我也会取消你的资格并扣发你的零用钱。&lt;/p&gt;
&lt;p&gt;这类似于在学期初（即初始监督微调之后）标记一条“参考线”。你不能偏离这条原始策略太远，否则会面临惩罚。&lt;/p&gt;
&lt;h4 id="3-熵奖励"&gt;3. 熵奖励
&lt;/h4&gt;&lt;p&gt;熵奖励通过惩罚低熵来鼓励LLM的生成进行探索：&lt;/p&gt;
$$
H(\theta) = - \mathbb{E}_{a_t} [\log \pi_\theta (a_t | s_t)].
$$&lt;p&gt;&lt;strong&gt;伪代码：&lt;/strong&gt;&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;div class="chroma"&gt;
&lt;table class="lntable"&gt;&lt;tr&gt;&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code&gt;&lt;span class="lnt"&gt;1
&lt;/span&gt;&lt;span class="lnt"&gt;2
&lt;/span&gt;&lt;span class="lnt"&gt;3
&lt;/span&gt;&lt;span class="lnt"&gt;4
&lt;/span&gt;&lt;span class="lnt"&gt;5
&lt;/span&gt;&lt;span class="lnt"&gt;6
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class="lntd"&gt;
&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-python" data-lang="python"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="c1"&gt;# 计算当前策略的熵&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="n"&gt;probs_current&lt;/span&gt; &lt;span class="o"&gt;=&lt;/span&gt; &lt;span class="n"&gt;F&lt;/span&gt;&lt;span class="o"&gt;.&lt;/span&gt;&lt;span class="n"&gt;softmax&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;logits_current&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt; &lt;span class="n"&gt;dim&lt;/span&gt;&lt;span class="o"&gt;=-&lt;/span&gt;&lt;span class="mi"&gt;1&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="n"&gt;log_probs_current&lt;/span&gt; &lt;span class="o"&gt;=&lt;/span&gt; &lt;span class="n"&gt;F&lt;/span&gt;&lt;span class="o"&gt;.&lt;/span&gt;&lt;span class="n"&gt;log_softmax&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;logits_current&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt; &lt;span class="n"&gt;dim&lt;/span&gt;&lt;span class="o"&gt;=-&lt;/span&gt;&lt;span class="mi"&gt;1&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="n"&gt;entropy&lt;/span&gt; &lt;span class="o"&gt;=&lt;/span&gt; &lt;span class="o"&gt;-&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;probs_current&lt;/span&gt; &lt;span class="o"&gt;*&lt;/span&gt; &lt;span class="n"&gt;log_probs_current&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;&lt;span class="o"&gt;.&lt;/span&gt;&lt;span class="n"&gt;sum&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;dim&lt;/span&gt;&lt;span class="o"&gt;=-&lt;/span&gt;&lt;span class="mi"&gt;1&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="n"&gt;entropy_bonus&lt;/span&gt; &lt;span class="o"&gt;=&lt;/span&gt; &lt;span class="n"&gt;entropy&lt;/span&gt;&lt;span class="o"&gt;.&lt;/span&gt;&lt;span class="n"&gt;mean&lt;/span&gt;&lt;span class="p"&gt;()&lt;/span&gt; &lt;span class="c1"&gt;# 对序列和批次取平均&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;h4 id="最后ppo-目标函数"&gt;最后，PPO 目标函数
&lt;/h4&gt;&lt;p&gt;给定上述三个项，除了价值函数的 MSE 损失，PPO 目标函数定义如下：&lt;/p&gt;
$$
\mathcal{L}_{\text{PPO}}(\theta, \gamma) = \underbrace{\mathcal{L}_{\text{clip}}(\theta)}_{\text{最大化奖励}} + \underbrace{w_1 H(\theta)}_{\text{最大化熵}} - \underbrace{w_2 \text{KL}(\theta)}_{\text{惩罚KL散度}} - \underbrace{w_3 \mathcal{L(}\gamma)}_{\text{优化价值函数}}
$$&lt;p&gt;该目标函数中不同项的总结如下：&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;&lt;strong&gt;项&lt;/strong&gt;&lt;/th&gt;
&lt;th&gt;&lt;strong&gt;目的&lt;/strong&gt;&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;$\mathcal{L}_{\text{clip}}(\theta)$&lt;/td&gt;
&lt;td&gt;最大化高优势动作的奖励（裁剪以避免不稳定性）。&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;$H(\theta)$&lt;/td&gt;
&lt;td&gt;最大化熵以鼓励探索。&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;$\text{KL}(\theta)$&lt;/td&gt;
&lt;td&gt;惩罚与参考策略的偏差（稳定性）。&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;$\mathcal{L}(\gamma)$&lt;/td&gt;
&lt;td&gt;最小化价值函数预测误差（L2 损失）。&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;h2 id="grpo"&gt;GRPO
&lt;/h2&gt;&lt;p&gt;现在我们对PPO有了很好的理解，理解GRPO就非常简单了，关键的区别在于两种算法如何估计优势$A$：与PPO通过价值函数估计优势不同，GRPO通过使用相同的提示从LLM中采样多个响应来实现这一点。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;工作流程：&lt;/strong&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;对于每个提示$p$，从LLM策略$\pi_\theta$中抽取一组$N$个响应$\mathcal{G}={r_1, r_2,&amp;hellip;r_N}$；&lt;/li&gt;
&lt;li&gt;使用奖励模型$R_\phi$计算每个响应的奖励${R_\phi(r_1),R_\phi(r_2),&amp;hellip;R_\phi(r_N)}$；&lt;/li&gt;
&lt;li&gt;计算每个响应的组内归一化优势：
$$
A_i = \frac{R_\phi(r_i) - \text{mean}(\mathcal{G})}{\text{std}(\mathcal{G})},
$$
其中$\text{mean}(\mathcal{G})$和$\text{std}(\mathcal{G})$分别表示组内的均值和标准差。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;在GRPO中，优势近似为每个响应在其响应组内的归一化奖励。这消除了使用价值函数计算每步奖励的需求，更不用说数学上的简单和优雅了。这不禁让人问——为什么我们没有早点这样做？&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;更新&lt;/strong&gt;：来自&lt;a class="link" href="https://x.com/him_sahni" target="_blank" rel="noopener"
&gt;@him_sahni&lt;/a&gt;的一些见解，他“在前世做过强化学习”：&lt;strong&gt;“为什么没有人尝试过GRPO”的原因是——我们确实尝试过&lt;/strong&gt;。在强化学习中，你通过减去一个基线（通常是几个轨迹的平均奖励）来更新策略以减少方差。事实上，理论表明理想的基线是从状态开始预期的未来总奖励，通常称为“价值”。使用价值函数作为基线的方法被称为actor-critic方法，而PPO是其稳定版本。现在，在传统的强化学习中，基线可以是当前状态的任何函数，传统上只是单个批次中轨迹的奖励；在GRPO中，这个基线是针对每个提示生成的1000个样本计算的，这是新颖的。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;直觉上就是：有一天，爸爸说：“我没有时间一直评估你的学习进度并画新的分数线。为什么不先做五套模拟测试，然后用它们的平均分作为你的预期分数呢？如果你在真正的测试中超过了这个平均分，那就说明你比自己的预期做得更好，所以我就会奖励你。否则，你不会得到太多。”我和哥哥，甚至可能还有更多的同学，都可以依靠各自的一套模拟测试，而不是一个爸爸需要不断调整的“价值函数”。&lt;/p&gt;
&lt;h4 id="grpo目标"&gt;GRPO目标
&lt;/h4&gt;&lt;p&gt;与PPO类似，GRPO仍然使用&lt;strong&gt;剪裁替代损失&lt;/strong&gt;以及&lt;strong&gt;KL惩罚&lt;/strong&gt;。这里没有使用熵奖励项，因为基于组的采样已经鼓励了探索。剪裁替代损失与PPO中使用的完全相同，但为了完整性，这里再次给出：&lt;/p&gt;
$$
\begin{align*}
&amp; \mathcal{L}_{\text{clip}}(\theta) = \\
&amp;\frac{1}{N} \sum_{i=1}^N \left( \min\left( \frac{\pi_\theta(r_i|p)}{\pi_{\theta_{\text{old}}}(r_i|p)} A_i, \ \text{clip}\left( \frac{\pi_\theta(r_i|p)}{\pi_{\theta_{\text{old}}}(r_i|p)}, 1-\epsilon, 1+\epsilon \right) A_i \right) \right),
\end{align*}
$$&lt;p&gt;然后，加上KL惩罚项，最终的GRPO目标可以写为：&lt;/p&gt;
$$
\mathcal{L}_{\text{GRPO}}(\theta) = \underbrace{\mathcal{L}_{\text{clip}}(\theta)}_{\text{最大化奖励}} - \underbrace{w_1\mathbb{D}_{\text{KL}}(\pi_\theta || \pi_{\text{orig}})}_{\text{惩罚KL散度}}
$$&lt;h2 id="关于r1的更多思考残酷的简洁"&gt;关于R1的更多思考：残酷的简洁
&lt;/h2&gt;&lt;p&gt;最后，关于R1再多说几句。&lt;/p&gt;
&lt;p&gt;无论是否被过度吹捧，从论文中可以明显看出，R1在LLM训练中采用了&lt;strong&gt;剥离一切冗余、直截了当的方法&lt;/strong&gt;，优先考虑残酷的简洁而非复杂性。GRPO只是冰山一角。以下是更多体现其残酷简洁的例子：&lt;/p&gt;
&lt;h3 id="1-基于规则的确定性奖励"&gt;&lt;strong&gt;1. 基于规则的确定性奖励&lt;/strong&gt;
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;是什么&lt;/strong&gt;：放弃神经过程奖励模型（PRM）或结果奖励模型（ORM）。使用&lt;strong&gt;二元检查&lt;/strong&gt;，包括：
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;答案正确性&lt;/strong&gt;：最终答案与标准答案匹配（例如，数学解题、代码编译）。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;格式化&lt;/strong&gt;：强制答案使用&lt;code&gt;&amp;lt;think&amp;gt;...&amp;lt;/think&amp;gt;&amp;lt;answer&amp;gt;...&amp;lt;/answer&amp;gt;&lt;/code&gt;模板。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;语言一致性&lt;/strong&gt;：惩罚混合语言输出（例如，用英语推理回答中文问题）。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;为什么&lt;/strong&gt;：确定性规则避免了&lt;strong&gt;奖励操纵&lt;/strong&gt;（例如，模型通过看似合理但错误的步骤欺骗神经奖励模型），并消除了奖励模型的训练成本。&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="2-冷启动数据最少的人工干预"&gt;&lt;strong&gt;2. 冷启动数据：最少的人工干预&lt;/strong&gt;
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;是什么&lt;/strong&gt;：不使用大规模的SFT数据集，而是通过以下方式收集&lt;strong&gt;几千个高质量的CoT示例&lt;/strong&gt;：
&lt;ul&gt;
&lt;li&gt;使用少量示例提示基础模型。&lt;/li&gt;
&lt;li&gt;轻量级的人工后期处理（例如，添加Markdown格式）。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;为什么&lt;/strong&gt;：避免了昂贵的SFT阶段，同时为RL提供了“足够好”的起点。&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="3-拒绝采样过滤困难训练更难"&gt;&lt;strong&gt;3. 拒绝采样：过滤困难，训练更难&lt;/strong&gt;
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;是什么&lt;/strong&gt;：在RL训练后，生成&lt;strong&gt;60万个推理轨迹&lt;/strong&gt;，然后&lt;strong&gt;丢弃所有错误响应&lt;/strong&gt;。只保留“胜者”（正确答案）用于监督微调（SFT）。没有复杂的重新排序，没有偏好对。仅仅是适者生存的过滤。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;为什么&lt;/strong&gt;：它有效，为什么不呢！&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="4-蒸馏复制粘贴推理"&gt;&lt;strong&gt;4. 蒸馏：复制粘贴推理&lt;/strong&gt;
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;是什么&lt;/strong&gt;：为了训练较小的模型，直接在DeepSeek-R1生成的&lt;strong&gt;80万个响应&lt;/strong&gt;上进行微调。没有RL，没有迭代对齐——只是模仿。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;为什么&lt;/strong&gt;：较小的模型继承了较大模型通过暴力RL发现的推理模式，从而绕过了小型部署中的昂贵RL。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;DeepSeek R1的设计反映了人工智能领域的一个更广泛的趋势：&lt;strong&gt;规模和简洁往往胜过聪明的工程&lt;/strong&gt;。通过无情地简化：用规则替换学习组件，利用大规模并行采样，并锚定在预训练的基线上，R1以更少的失败模式实现了SOTA结果。它并不优雅，但它是&lt;em&gt;有效的&lt;/em&gt;。&lt;/p&gt;
&lt;p&gt;谁会想到激励良好思考的最佳方式是：&lt;strong&gt;停止过度思考&lt;/strong&gt;。&lt;/p&gt;</description></item></channel></rss>